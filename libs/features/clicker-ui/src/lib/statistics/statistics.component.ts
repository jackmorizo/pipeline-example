import { Component, ViewEncapsulation } from '@angular/core';

@Component({
  selector: 'clickboard-stats',
  template: `clickboard-stats comes here`,
  encapsulation: ViewEncapsulation.None,
})
export class ClickBordStatisticsComponent {}
