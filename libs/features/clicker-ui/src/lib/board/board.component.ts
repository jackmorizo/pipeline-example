import { Component, ViewEncapsulation } from '@angular/core';

@Component({
  selector: 'clickboard',
  template: `clickboard comes here`,
  encapsulation: ViewEncapsulation.None,
})
export class ClickBordComponent {}
